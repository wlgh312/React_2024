// ESM - ECMAScript Module

// 개별 Export
const name = '놀부';
const age = 30;
const check = true;
const arr = [10, 20];
const user = { name: '흥부', age: 20 };
const onAdd = (x, y) => `${x} + ${y} = ${x + y}`;

// 묶어서 개별 Export

